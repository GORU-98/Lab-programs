#include<stdio.h>
int main (){
    char arr[]="Hey I am doing it to find the length";
    int a=18;
    int i;
    // printf("%d",a);
    i=0;
    while(arr[i] ? " "){
        i++;
    }
    printf("\n%d",i);
    // printf("%d",l);

    return 0;
}